create a personnel page that gets a perosnnel id and load a personnel, display all of his details and allow edit them
add preferedContaact field that can be either : 'EMAIL', 'WHATSAPP', 'PHONE', 'INSTAGRAM', 'FACEBOOK', ...etc (deffault is phone) .
this should represent how the personel wants to be contacted,
when for example the user wants to get contacted via mail, make the mail input mandatory
add an upload profile picture

make this page accessable only via a logged in member of an agency

integration with rest of the plateform
we have this frontend\src\app\shared\components\phone-input\phone-input.component.ts component injected everywhere that we need a personnel phone, add in that component two icons, one to navigate to personnel details page , and one icon that shows perosnnel preferred contact
for the icon that represents preferedContaact, try o launch an intent if it is a mobile plateform or open a new tab if it is a web plateform so it looks premium